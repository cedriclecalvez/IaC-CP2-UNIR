all:
  children:
    vm:
      hosts:
        vm-server:
          ansible_host: "9.163.5.88"
          ansible_user: "adminuser"
          ansible_ssh_private_key_file: "/root/.ssh/id_rsa"
    aks:
      hosts:
        aks-server:
          ansible_host: "aks-unir"